import React from 'react';
import { DescSection } from './Desc.styled';

const Desc = () => {
  return (
    <DescSection>
      {/* Your component code here */}
    </DescSection>
  );
};

export default Desc;