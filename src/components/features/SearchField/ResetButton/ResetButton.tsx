import React from 'react';
import { Reset, ResetIcon } from './ResetButton.styled';

const ResetButton = () => {
  return (
    <Reset>
      <ResetIcon />
      초기화
    </Reset>
  );
};

export default ResetButton;
